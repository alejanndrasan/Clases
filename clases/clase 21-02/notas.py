'''Las clases van con la primera letra en mayuscula.
El self no hace falta, pero es para senalar que es de la clase, pero se pueden escribir solo como variable
Para llamar a la clase, llamar al constructor, se anota como cuando llamas una funcion y le pasas los parametros
Para cambiar alguno de los atributos, llamas al objeto.atributo = "cambio"
Hacer atributos privados para encapsular la clase.

Herencia:
Se puede reescribir metodos para no crear metodos adicionales.
Se puede sobrecargar a un metodo, es decir, cambiarle el numero de parametros (cuando se llama).
Con la palabra clave super, podemos llamar los metodos de la clase padre a la clase hija.

    Para indicar Herencia:
        class Vehicle:
        
        class Car(Vehicle): #ahi se crea el hijo de Vehicle

Polimorfismo:
Cuando se cambia un poco el metodo en uno de las clases hijas.

INVESTIGACION:
Hasta el viernes que viene, vale 2 puntos para el siguiente parcial.
Se debe defender.'''

print('Clase 21-02')